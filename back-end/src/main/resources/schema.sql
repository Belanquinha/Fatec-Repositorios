-- Migrações antigas do modelo de gestor foram removidas junto com o papel GESTOR.
-- O schema é gerenciado pelo Hibernate (ddl-auto: update).
-- Script vazio (apenas comentários) é rejeitado pelo inicializador do Spring;
-- este no-op mantém o modo always sem efeito.
SELECT 1;