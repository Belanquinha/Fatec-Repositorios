---
scope: Fatec-Repositorios: postagem e aprovacao de projetos (aluno/professor/admin, sem gestor)
purpose: build substrate para epics e implementacao
altitude: feature
updated: 2026-09-16T17:06
---

- (decision) Paradigm: layered (Controller-Service-Repository) REST API on Spring Boot + Angular SPA + Stateless JWT - adopted from existing codebase (brownfield ratify)
- (decision) AD-1 [ADOPTED] backend is the security and role authority: issues its own HMAC JWT (auth0), never trusts front-end role claims
- (decision) AD-2 role is derived at login by UPN domain from Graph /me: @aluno.cps.sp.gov.br checked before @cps.sp.gov.br, other CPS mailbox falls back to aluno; UserRole becomes ADMIN/ALUNO/PROFESSOR (GESTOR removed) - prevents two builders storing contradictory roles
- (decision) AD-3 MSAL pinned to CPS tenant (authority GUID 2f8088c4-... in docker-compose) and app registration tenant-restricted; backend rejects issuer/tenant outside CPS - prevents external accounts logging in as students
- (decision) AD-4 [PRD NFR3] professor-institution link is a join table capped at 4 rows, enforced server-side - prevents one professor concentrating the system
- (decision) AD-5 institutions are catalog data: admin-only mutation, reads open to authenticated users
- (decision) AD-6 Projeto requires professorResponsavelId NOT NULL at creation (immediate owner); posting sets estado AGUARDANDO_APROVACAO
- (decision) AD-7 project lifecycle state machine RASCUNHO to AGUARDANDO_APROVACAO to APROVADO|REJEITADO; only professor(s) bound to the project institution may transition; rejection requires motivo
- (constraint) Legacy gestor flow must be removed from backend (Gestor model/repo, /auth/login senha, UserRole.GESTOR, SecurityConfig role matcher) and front (login-instituicao route, usuarioRole GESTOR)
- (version) Stack reality-checked from repo: Spring Boot 4.1.0/Java 21, auth0 java-jwt 4.4.0 and jwks-rsa 0.22.0, springdoc-openapi 3.0.2, Postgres 17-alpine, Angular 21, msal-angular 5.3.1/msal-browser 5.16.0, docker-compose
- (question) Who may approve: only the responsible professor (recommended) or any professor linked to the institution?
- (direction) Deferred: exact approver policy, moderation/disputes owner, admin provisioning mechanism (seed init SQL exists), photo storage (Graph base64 vs file), pagination and search, public portfolio
- (event) Self-review passed: versions verified from repo (pom/package.json/compose, no training-data claims); adversarial check no contradictions beyond Deferred approver policy already flagged; lint_spine 0 findings (fixed ERD edge label typo)
